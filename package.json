{
  "name": "averaging",
  "description": "Functions to run daily to calculate averages",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "athena-express": "6.0.3",
    "aws-sdk": "^2.442.0",
    "knex": "^0.21.1",
    "knex-postgis": "^0.12.0",
    "pg": "^8.2.1"
  },
  "devDependencies": {
    "jest": "^24.7.1",
    "aws-sdk-mock": "^4.5.0"
  },
  "scripts": {
    "test": "jest",
    "local": "sam build && sam local invoke --env-vars env.json --docker-network host "
  }
}